# Step 1: Add targeted MCP companion skill selection

## Objective
When `sibu mcp use github` or `sibu mcp use notion` installs a target MCP server, also install and record the matching dedicated export workflow skill.

## Tasks
- Derive companion export skills from selected MCP servers.
- Render newly required skill files and refresh affected MCP config files plus AGENTS.md.
- Preserve readiness and local-edit protections for all affected files.
- Record selected MCP servers, selected workflow skills, managed files, and MCP config values in one state update.

## Verification
- Targeted MCP tests cover GitHub, Notion, and combined MCP selection behavior.

## Review Status
Approved on 2026-05-19T13:59:00-06:00.
