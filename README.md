# ai-augmented-workflow

A local workspace for developing and refining an AI-augmented software development workflow.

## CLI

This first iteration focuses only on initializing brand-new projects with an `AGENTS.md` file.

From a project directory, run:

```sh
npx ekko init
```

The CLI asks for a project overview and writes an `AGENTS.md` file based on the template in this repository.
